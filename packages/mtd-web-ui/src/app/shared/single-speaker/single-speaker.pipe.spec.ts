import { SingleSpeakerPipe } from './single-speaker.pipe';

describe('SingleSpeakerPipe', () => {
  it('create an instance', () => {
    const pipe = new SingleSpeakerPipe();
    expect(pipe).toBeTruthy();
  });
});
